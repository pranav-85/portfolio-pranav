/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/portfolio-pranav",
  assetPrefix: "/portfolio-pranav/",
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
